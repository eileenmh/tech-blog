 <header>
        <h1 class="title">The Tech Blog</h1>
</header>

<div class="container">
    <section class="articles">
        {{#each articles as |article|}}
        {{> article-card}}
        {{/each}}
    </section>
</div>